/// <reference types="cypress" />

// Custom commands for Cypress tests
declare global {
  namespace Cypress {
    interface Chainable {
      // Add custom commands here if needed
    }
  }
}

export {};
